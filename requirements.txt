# Pixel Quest AI — Python dependencies

# ── Web server ──────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# ── AI / LLM ────────────────────────────────────────────────────────────────
openai>=1.0.0          # DeepSeek is compatible with OpenAI SDK

# ── Semantic search (optional — improves search quality) ───────────────────
# Removed local semantic embeddings in favor of LLM function calling


# ── HTTP client (wiki scraping / sync) ─────────────────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0

# ── Utilities ───────────────────────────────────────────────────────────────
python-dotenv>=1.0.0   # Loads .env file automatically
